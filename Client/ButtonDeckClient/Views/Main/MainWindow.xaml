<Window x:Class="ButtonDeckClient.Views.Main.MainWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ButtonDeckClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="458" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Connection">
                <MenuItem Header="Arduino" ItemsSource="{Binding SerialPorts.Value}">
                    <!--<MenuItem.ItemContainerStyle>
                        <Style TargetType="MenuItem">
                            <Setter Property="Header" Value="{Binding}"></Setter>
                        </Style>
                    </MenuItem.ItemContainerStyle>-->
                </MenuItem>
                <MenuItem Header="OBS..."></MenuItem>
            </MenuItem>
        </Menu>
        <Grid/>
    </DockPanel>
</Window>
